import React from 'react';

export function AdminDashboard({go,stats={}}){const items=[['overview','Overview','📊'],['users','Users','👥'],['sellers','Sellers','🌾'],['applications','Seller applications','📝'],['payments','Payments','💳'],['listings','Listings','🛒'],['orders','Orders','📦'],['messages','Messages & reports','💬'],['notifications','Notifications','🔔'],['categories','Categories','🏷️'],['reviews','Reviews','⭐'],['settings','Platform settings','⚙️']];return <section className="adminPage"><div className="adminHero"><div><span>FARMMARKET UGANDA · ADMIN</span><h1>Super Admin</h1><p>Manage the marketplace, sellers, users, payments and activity from one place.</p></div><button onClick={()=>go('dashboard')}>← My account</button></div><div className="adminStats"><Stat n={stats.users||0} t="Users"/><Stat n={stats.sellers||0} t="Sellers"/><Stat n={stats.listings||0} t="Listings"/><Stat n={stats.orders||0} t="Orders"/><Stat n={stats.pending||0} t="Pending"/><Stat n={stats.revenue||'UGX 0'} t="Revenue"/></div><div className="adminLayout"><aside>{items.map(([id,title,icon])=><button key={id} onClick={()=>go('admin/'+id)}><span>{icon}</span>{title}<b>›</b></button>)}</aside><div className="adminContent"><div className="adminWelcome"><span>CONTROL CENTRE</span><h2>Marketplace overview</h2><p>Choose an area above to manage FarmMarket Uganda.</p></div><div className="adminCards"><AdminCard title="Seller applications" value={stats.pending||0} text="Applications waiting for review" go={()=>go('admin/applications')}/><AdminCard title="Listings" value={stats.listings||0} text="Marketplace listings to manage" go={()=>go('admin/listings')}/><AdminCard title="Payments" value={stats.payments||0} text="Seller payments awaiting verification" go={()=>go('admin/payments')}/><AdminCard title="Reports" value={stats.reports||0} text="User reports requiring attention" go={()=>go('admin/messages')}/></div></div></div><nav className="adminMobileNav">{items.slice(0,5).map(([id,title,icon])=><button key={id} onClick={()=>go('admin/'+id)}><span>{icon}</span><small>{title}</small></button>)}</nav></section>}
function Stat({n,t}){return <div className="adminStat"><b>{n}</b><span>{t}</span></div>};function AdminCard({title,value,text,go}){return <button className="adminCard" onClick={go}><div><span>{title}</span><strong>{value}</strong><p>{text}</p></div><b>→</b></button>}
