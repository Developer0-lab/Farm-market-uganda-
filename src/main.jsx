import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import{Search,Plus,Heart,MessageCircle,UserCircle,Menu,X,Leaf,LogIn,LogOut,ShieldCheck,Package,ChevronRight,ArrowLeft,Bell,ShoppingCart,Settings,Store,ClipboardList}from'lucide-react';import'./styles.css';
const U=import.meta.env.VITE_SUPABASE_URL,K=import.meta.env.VITE_SUPABASE_ANON_KEY;const db=U&&K?createClient(U,K,{db:{schema:'farm_market'}}):null;const money=n=>new Intl.NumberFormat('en-UG',{style:'currency',currency:'UGX',maximumFractionDigits:0}).format(n||0);const demo=[['1','Fresh Matooke',35000,'bunch','Wakiso','Produce','🍌','Kato Farm'],['2','Free-range Eggs',12000,'tray','Mukono','Livestock','🥚','Green Valley Farm'],['3','Fresh Tomatoes',8000,'kg','Mbarara','Produce','🍅','Ankole Growers'],['4','Local Chicken',28000,'bird','Luweero','Livestock','🐔','Nile Farm']].map(x=>({id:x[0],title:x[1],price:x[2],unit:x[3],district:x[4],category:x[5],emoji:x[6],seller:x[7]}));
function App(){const[page,setPage]=useState(location.hash.slice(1)||'home'),[session,setSession]=useState(null),[profile,setProfile]=useState(null),[listings,setListings]=useState(demo),[auth,setAuth]=useState(false),[mode,setMode]=useState('signup'),[q,setQ]=useState(''),[saved,setSaved]=useState([]),[notes,setNotes]=useState([]),[menu,setMenu]=useState(false),[toast,setToast]=useState('');const go=p=>{location.hash=p;setPage(p);setMenu(false);scrollTo(0,0)};useEffect(()=>{const h=()=>setPage(location.hash.slice(1)||'home');addEventListener('hashchange',h);if(!db)return()=>removeEventListener('hashchange',h);db.auth.getSession().then(({data})=>setSession(data.session));const{data}=db.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>{removeEventListener('hashchange',h);data.subscription.unsubscribe()}},[]);useEffect(()=>{if(!db||!session)return;db.from('profiles').select('*').eq('id',session.user.id).maybeSingle().then(({data})=>setProfile(data));db.from('notifications').select('*').is('read_at',null).order('created_at',{ascending:false}).then(({data})=>setNotes(data||[]))},[session]);useEffect(()=>{if(!db)return;db.from('listings').select('*').eq('status','approved').order('created_at',{ascending:false}).then(({data})=>{if(data?.length)setListings(data.map(x=>({...x,emoji:'🌾',seller:'Verified farmer'})))})},[]);const logout=async()=>{await db?.auth.signOut();setSession(null);setProfile(null);go('home')};const filtered=listings.filter(x=>`${x.title} ${x.district} ${x.category} ${x.seller}`.toLowerCase().includes(q.toLowerCase()));
const Header=()=> <header><div className="wrap nav"><button className="icon mobile" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button><button className="brand" onClick={()=>go('home')}><span className="logo"><Leaf/></span><b>FarmMarket</b><span>Uganda</span></button><nav className={menu?'open':''}><button onClick={()=>go('home')}>Home</button><button onClick={()=>go('market')}>Marketplace</button><button onClick={()=>go('how')}>How it works</button><button onClick={()=>go('about')}>About</button></nav><div className="actions"><button className="sell" onClick={()=>session?go('seller'):setAuth(true)}><Plus/> Become a seller</button>{session?<><button className="icon" onClick={()=>go('notifications')}><Bell/>{notes.length>0&&<b className="badge">{notes.length}</b>}</button><button className="icon" onClick={()=>go('dashboard')}><UserCircle/></button></>:<><button className="signin" onClick={()=>{setMode('login');setAuth(true)}}><LogIn/> Sign in</button><button className="signup" onClick={()=>{setMode('signup');setAuth(true)}}>Create account</button></>}</div></div></header>;
const Card=({x})=><article className="card"><button className="heart" onClick={()=>session?setSaved(a=>a.includes(x.id)?a.filter(i=>i!==x.id):[...a,x.id]):(setMode('login'),setAuth(true))}><Heart fill={saved.includes(x.id)?'currentColor':'none'}/></button><div className="photo">{x.emoji}</div><div className="cardbody"><small>{x.category}</small><h3>{x.title}</h3><strong>{money(x.price)} <i>/ {x.unit}</i></strong><p>{x.district} · {x.seller}</p><button className="contact" onClick={()=>session?go('messages'):(setMode('login'),setAuth(true))}><MessageCircle/> Contact seller</button></div></article>;
function Home(){return <><section className="hero"><div className="wrap heroGrid"><div><label>UGANDA'S FARM-TO-MARKET PLATFORM</label><h1>Buy fresh.<br/><em>Sell smarter.</em></h1><p>Connect farmers, buyers and fresh produce across Uganda in one trusted marketplace.</p><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search produce, livestock, location..."/><button onClick={()=>go('market')}>Search</button></div><div className="heroActions"><button className="signup" onClick={()=>{setMode('signup');setAuth(true)}}>Create account</button><button className="outline" onClick={()=>go('market')}>Browse marketplace</button></div></div><div className="heroVisual">🌾<b>Fresh from Ugandan farms</b><span>Local sellers · Fair prices · Nearby produce</span></div></div></section><section className="wrap section"><div className="sectionhead"><div><label>MARKETPLACE</label><h2>Popular today</h2></div><button className="outline" onClick={()=>go('market')}>View marketplace <ChevronRight/></button></div><div className="grid">{listings.map(x=><Card x={x} key={x.id}/>)}</div></section></>}
function Market(){return <section className="wrap section page"><button className="back" onClick={()=>go('home')}><ArrowLeft/> Home</button><label>MARKETPLACE</label><h2>Find farm products</h2><div className="search wide"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products, districts, categories..."/></div><div className="grid">{filtered.map(x=><Card x={x} key={x.id}/>)}</div></section>}
function Auth(){const[email,setEmail]=useState(''),[pw,setPw]=useState('');const submit=async e=>{e.preventDefault();if(!db)return setToast('Supabase is not connected');const r=mode==='login'?await db.auth.signInWithPassword({email,password:pw}):await db.auth.signUp({email,password:pw});if(r.error)setToast(r.error.message);else{setAuth(false);setToast(mode==='login'?'Welcome back':'Account created. Check your email if confirmation is enabled.');go('dashboard')}};return <div className="modal"><form className="auth" onSubmit={submit}><button type="button" className="close" onClick={()=>setAuth(false)}><X/></button><span className="logo"><Leaf/></span><h2>{mode==='login'?'Welcome back':'Create your Farm Market account'}</h2><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength="6" value={pw} onChange={e=>setPw(e.target.value)}/></label><button className="sell full">{mode==='login'?'Sign in':'Create account'}</button><button type="button" className="switch" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'New here? Create an account':'Already have an account? Sign in'}</button></form></div>}
function Dashboard(){return <section className="wrap page"><div className="dashTitle"><div><label>MY ACCOUNT</label><h2>Welcome{session?.user?.email?', '+session.user.email.split('@')[0]:''}</h2></div><button className="outline" onClick={logout}><LogOut/> Sign out</button></div><div className="dashgrid"><Tile icon={<ShoppingCart/>} title="My orders" go={()=>go('orders')}/><Tile icon={<Store/>} title="Seller centre" go={()=>go('seller')}/><Tile icon={<MessageCircle/>} title="Messages" go={()=>go('messages')}/><Tile icon={<Heart/>} title="Saved listings" go={()=>go('favorites')}/><Tile icon={<Settings/>} title="My profile" go={()=>go('profile')}/>{profile?.role==='admin'&&<Tile icon={<ShieldCheck/>} title="Super Admin" go={()=>go('admin')}/>}</div></section>};function Tile({icon,title,go}){return <button className="tile" onClick={go}>{icon}<b>{title}</b><ChevronRight/></button>}
function Seller(){return <section className="wrap page"><button className="back" onClick={()=>go('dashboard')}><ArrowLeft/> Dashboard</button><div className="formlayout"><div><label>SELLER CENTRE</label><h2>Become a seller</h2><p>List your farm products and connect with buyers across Uganda.</p></div><form className="panel" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);const r=await db.from('sellers').insert({user_id:session.user.id,farm_name:f.get('farm'),district:f.get('district'),phone:f.get('phone'),activation_transaction_id:f.get('tx'),activation_payment_status:'pending'});setToast(r.error?r.error.message:'Seller application submitted.');if(!r.error)go('dashboard')}}><h3>Seller application</h3><label>Farm / business name<input name="farm" required/></label><label>District<input name="district" required/></label><label>Phone<input name="phone" required/></label><label>Transaction ID<input name="tx"/></label><button className="sell full">Submit application</button></form></div></section>}
function Simple({title,icon,children}){return <section className="wrap page"><button className="back" onClick={()=>go('dashboard')}><ArrowLeft/> Dashboard</button><div className="panel center">{icon}<h2>{title}</h2>{children}</div></section>}
function Notifications(){return <Simple title="Notifications" icon={<Bell/>}>{notes.length?notes.map(n=><div className="notification" key={n.id}><b>{n.title}</b><p>{n.body}</p></div>):<p>You're all caught up.</p>}</Simple>};function Admin(){if(profile?.role!=='admin')return <Simple title="Super Admin" icon={<ShieldCheck/>}><p>Administrator access required.</p></Simple>;return <Simple title="Super Admin" icon={<ShieldCheck/>}><p>Platform control centre</p><div className="adminmenu"><button>Users <ChevronRight/></button><button>Seller applications <ChevronRight/></button><button>Listings <ChevronRight/></button><button>Reports <ChevronRight/></button><button>Payments <ChevronRight/></button></div></Simple>}
function Info({title,text}){return <section className="wrap page"><div className="panel info"><label>FARMMARKET UGANDA</label><h2>{title}</h2><p>{text}</p></div></section>}
function AppPage(){switch(page){case'market':return <Market/>;case'dashboard':return session?<Dashboard/>:<Simple title="Sign in required" icon={<UserCircle/>}><button className="signup" onClick={()=>setAuth(true)}>Sign in / Create account</button></Simple>;case'seller':return session?<Seller/>:<Simple title="Become a seller" icon={<Store/>}><button className="signup" onClick={()=>setAuth(true)}>Create account</button></Simple>;case'notifications':return session?<Notifications/>:<Simple title="Sign in required" icon={<Bell/>/>;case'admin':return <Admin/>;case'orders':return <Simple title="My orders" icon={<ClipboardList/>}><p>No orders yet.</p><button className="sell" onClick={()=>go('market')}>Start shopping</button></Simple>;case'messages':return <Simple title="Messages" icon={<MessageCircle/>}><p>No conversations yet.</p></Simple>;case'favorites':return <Simple title="Saved listings" icon={<Heart/>}><p>{saved.length} saved listings.</p></Simple>;case'profile':return <Simple title="My profile" icon={<UserCircle/>}><p>{session?.user?.email}</p></Simple>;case'how':return <Info title="How it works" text="Browse farm products, create an account, contact sellers, and become a verified seller to list your own products."/>;case'about':return <Info title="About Farm Market Uganda" text="A marketplace connecting Ugandan farmers, sellers and buyers."/>;default:return <Home/>}}
return <><Header/><main><AppPage/></main>{auth&&<Auth/>}{toast&&<button className="toast" onClick={()=>setToast('')}>{toast}</button>}</>};createRoot(document.getElementById('root')).render(<App/>);